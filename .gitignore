*.raw
*.bin
*.zip
*.mbn
*.tgz
build
dist
files
mnt
rootfs
apk.static

# prebuilt directory for manual firmware (legacy, not used anymore)
prebuilt/

# keep committed firmware blobs (exception to *.bin above)
!firmware/
